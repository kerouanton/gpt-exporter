"""Legacy DOCX migration/reconstruction maintenance tools.

These modules are intentionally kept out of the repository root because the one-time
legacy reconstruction project is complete. Run them from the repository root with
`py -m tools.legacy.<module>` when maintenance or recovery is needed.
"""
